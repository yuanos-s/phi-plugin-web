[project]
name = "phi-plugin-web"
version = "3.0.0"
description = "Phigros 查分网页版 — FastAPI + TapTap + Supabase (无 LeanCloud)"
license = "GPL-3.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]",
    "httpx",
    "pycryptodome",
    "pyyaml",
    "python-multipart",
]

[tool.uvicorn]
host = "0.0.0.0"
port = 8000
